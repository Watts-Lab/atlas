{
  "name": "atlas",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev:api": "cd server && node server.js --prefix api",
    "dev:client": "cd client && npm run dev --prefix client",
    "dev": "npm run dev:api && npm run dev:client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
